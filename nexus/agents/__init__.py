"""ONEXUS-Agents catalog integration.

Reads the ONEXUS-Agents catalog (a directory of per-category JSON files)
and exposes agent metadata to the kernel for discovery and dispatch.

Set NEXUS_AGENTS_CATALOG to the path of a cloned ONEXUS-Agents repo
(or just its catalog/ directory) to enable.
"""
